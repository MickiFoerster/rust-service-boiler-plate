/*

use std::net::SocketAddr;

use anyhow::Result;
use axum::{
    routing::{get, post, IntoMakeService},
    Router,
};
use sqlx::PgPool;

#[derive(Clone)]
pub struct AppState {
    pub db_pool: PgPool,
}

pub fn run(service_address: &str, db_pool: PgPool) -> Result<()> {
    Ok(())
}

*/
